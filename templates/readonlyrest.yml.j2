---
readonlyrest:
  access_control_rules:
{% for var in creds %}

  - name: "Require HTTP Basic Auth for {{ creds[0].username }}"
    type: allow
    auth_key: "{{ var.username }}:{{ var.password }}"
{% endfor %}